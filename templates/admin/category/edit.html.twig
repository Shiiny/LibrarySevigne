{% extends 'admin/base.html.twig' %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin.category.index') }}">Toutes les catégories</a>
    </li>
    <li class="breadcrumb-item active">Éditer une catégorie</li>
{% endblock %}

{% block content %}
    <div class="card mb-3">
        <h2 class="card-header">Éditer une catégorie</h2>
        <div class="card-body">
            {{ include ('admin/category/_form.html.twig', {form: form, button: 'Enregistrer'}) }}
        </div>
        <div class="card-footer small text-muted">Actualisé le : {{ "now"|localizeddate('long', 'none') }}</div>
    </div>
{% endblock %}